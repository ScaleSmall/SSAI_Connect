name: Retired legacy Shared package consumer

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    name: Legacy consumer remains disabled and inert
    if: ${{ false }}
    runs-on: ubuntu-24.04
    timeout-minutes: 1
    steps:
      - name: Explain retirement
        run: |
          echo "This legacy workflow identity is retired."
          echo "Use Pull Shared with protected local evidence."
